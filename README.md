# 🚀 Multi-Branch Deploy to GitHub Pages

- `main` → Production deploy to GitHub Pages  
- `dev` → QA Preview deploy  
- Pull Requests → Run lint + test only (no deploy)  

## Run locally
```bash
npm install
npm run dev
